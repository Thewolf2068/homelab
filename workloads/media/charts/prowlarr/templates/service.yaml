apiVersion: v1
kind: Service
metadata:
  name: prowlarr
spec:
  type: ClusterIP
  selector:
    app: prowlarr
  ports:
    - port: {{ .Values.prowlarr.port }}
      targetPort: {{ .Values.prowlarr.port }}
