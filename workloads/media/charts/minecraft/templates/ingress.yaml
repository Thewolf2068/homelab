apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: {{ .Chart.Name }}
spec:
  entryPoints:
    - {{ .Values.subdomain }}
  routes:
    - match: HostSNI(`*`)
      services:
        - name: {{ .Chart.Name }}
          port: {{ .Values.port }}
